# 后端领域设计

## 统一语言

- 预约：客户对某项服务和时间的有效请求。
- 订单：预约被接受后形成的履约与结算记录。

## 领域边界

- 预约上下文负责可用性、预约状态和时间冲突规则。
- 订单上下文负责履约、价格和结算状态。

## 依赖规则

- 接口层调用应用层，应用层编排领域对象和端口。
- 领域层不依赖 Web 框架、数据库客户端或消息代理。
- 基础设施层实现 Repository 和外部系统端口。

## 一致性

- 一个事务只修改一个聚合。
- 跨聚合和跨上下文变化通过领域事件协调，并明确幂等、重试和补偿规则。
