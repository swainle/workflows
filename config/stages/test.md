- 依据根层规范、TC 和 Dev 实现执行真实测试与最小回归。
- 使用根层 `compose.yml` 与 `test.env` 创建隔离测试环境，不修改它们。
- 固定使用 `test/report.md` 记录覆盖、命令证据、缺陷、未执行项和结论。
- 测试结果必须经使用者人工审核；没有执行证据和确认不得写 `passed`。
- 不修改源码、需求根层规范或全局文件；实现问题返回 Dev，规范问题返回 Design。
- 只更新 `status.json` 的 Test 状态与证据。
