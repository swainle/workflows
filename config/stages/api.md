- OpenAPI 使用 JSON；异步事件契约使用 AsyncAPI JSON。
- 复用项目需求中已有的命名、错误和版本约定。
- 请求、响应和事件提供固定且可重复的 examples；覆盖正常、认证失败、权限不足、参数错误、冲突和限流场景。
- 提供与 Schema 一致的 Mock 数据和启动或生成方式，不使用真实用户数据；明确幂等、分页、版本和兼容性要求。
- 阶段产物必须列出契约校验和 Mock 校验方案。
- 不修改实现代码。
