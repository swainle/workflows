- 使用 Markdown 中的 Mermaid C4 图；按所需层级选择 `C4Context`、`C4Container`、`C4Component` 或 `C4Deployment`。
- 四个层级分别表达系统关系、容器职责、容器内部组件和部署拓扑；只生成本需求实际发生变化的层级。
- 只描述本需求涉及的系统边界、容器和依赖变化。
- 阶段产物必须列出边界、依赖方向、通信方式和数据所有权的验证规则。
- 不展开接口、表结构和实现细节。
