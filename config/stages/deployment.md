- 复用现有构建、发布、监控和回滚工具。
- Docker Compose 方案必须说明服务依赖、网络、Volume、健康检查、启动顺序和非敏感配置。
- 使用 Design 的 `production.compose.yml` 和 `production.env` 验证生产编排；发现错误时返回 Design 修正。
- 动态升级必须说明新旧实例切换、反向代理或负载均衡、兼容窗口和失败回滚；不得声称 Docker Compose 自动保证零停机。
- 数据库优先使用 expand-contract 迁移顺序；说明迁移前备份、恢复步骤、保留策略和恢复演练。
- 阶段产物必须包含部署、升级、迁移、备份、恢复、监控和回滚验证方案。
- 不写入密钥、令牌、证书或真实凭据。
- 以 Design 契约、Dev 实现结果和 Test 结论为依据，不重新定义业务行为。
- 只更新根层 `status.json` 的 Deployment 状态与证据；未通过或无法回滚时不得放行。
- 需要修改部署源码或配置时，在阶段产物中给出交由后续人工或开发执行的明确变更清单；当前阶段不直接修改源码。
