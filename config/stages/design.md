- 优先复用现有设计语言和 Design Tokens。
- 先表达共同体验，再记录各平台必要差异。
- 善用 Mermaid 流程图、时序图、状态图等表达关键交互；按问题选择最合适的图，不固定图表类型，文字足够时不画图。
- 覆盖页面结构、关键交互、加载与等待、空状态、异常、无权限、重试、响应式和可访问性；不得只设计成功状态。
- 阶段产物必须列出关键路径和各界面状态的可验证规则。
- 正式接口未定义且设计需要数据时，创建 `design/mock.json` 描述临时操作、数据和界面场景；不提前定义正式接口。
- 不在设计阶段修改源码。
