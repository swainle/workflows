# 本阶段要做什么

检查页面、组件、文字、交互状态、响应式布局和 Design Tokens。已有 `07-*` 文件可能被人工修改，必须一并优化。

# 允许修改

- `{{REQUIREMENT_DIR}}/07-design-tokens.json`
- `{{REQUIREMENT_DIR}}/07-*.md`
- 完成需求必须修改的前端源码和文字资源

不得修改后端、数据库或部署文件；全局 Design Tokens 变化写入 `07-frontend.git.patch`。
