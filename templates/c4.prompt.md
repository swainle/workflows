# 本阶段要做什么

根据 `issue/issue.md` 和业务流程确认系统边界、容器职责、依赖关系、通信方式和数据所有权。只描述本需求涉及的架构变化，不展开 API、表结构或代码实现细节；架构边界没有变化时可跳过本阶段。

# 允许修改

- `{{REQUIREMENT_DIR}}/c4/c4.puml`
- `{{REQUIREMENT_DIR}}/c4/*.md`

不得直接修改全局 C4 文件或业务源码；全局 C4 变化写入 `{{GLOBAL_PATCH_FILE}}`，且只能修改 `docs/architecture/c4.puml`。没有全局架构变化时不得创建空 Patch。
